import React from 'react'
import ReactDOM from 'react-dom/client'
import TruthTrace from './TruthTrace'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TruthTrace />
  </React.StrictMode>,
)
